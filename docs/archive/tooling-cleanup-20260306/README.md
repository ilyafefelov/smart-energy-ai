# Tooling Cleanup Archive

Archived on 2026-03-06 during repository cleanup.

This archive contains historical generated artifacts that were cluttering the active workspace but may still be useful for historical reference:

- `copilot-tracking/` - old plan, changes, prompt, detail, and research files generated during prior Copilot task sessions.
- `desloppify/` - generated external review packets, blind review packets, session logs, and subagent run outputs.
- `dashboard-docs/` - historical dashboard completion reports, session summaries, implementation plans, and one-off verification notes moved out of the active `dashboard/` folder.
- `nuxt-dashboard-docs/` - equivalent historical markdown artifacts moved out of the legacy `nuxt_dashboard/` folder.

Active code, current runtime configuration, and primary user-facing documentation remain in their original locations.
