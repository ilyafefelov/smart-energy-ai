# Reports Cleanup Archive

Archived on 2026-03-06 during the second documentation cleanup pass.

This archive contains the former `docs/reports/` collection, which had accumulated many overlapping completion summaries, session reports, testing notes, and one-off status documents. Those files were preserved for historical reference but moved out of the active documentation surface to reduce duplication and improve discoverability.

Active operator and architecture entry points now live in:

- `README.md`
- `QUICK_START.md`
- `docs/technical/`
- `docs/deployment/`

Historical reports remain available here under `reports/`.